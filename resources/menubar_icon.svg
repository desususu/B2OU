<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 44 44">
  <!-- macOS menu bar template icon — stroke-based bear glyph -->
  <!-- SF Symbol style: consistent 2px stroke, clean outlines -->
  <!-- System auto-inverts for dark mode when used as template image -->

  <!-- Left ear (drawn first; head's white fill hides internal overlap) -->
  <circle cx="14" cy="14" r="5" fill="none" stroke="#000000" stroke-width="2"/>

  <!-- Right ear -->
  <circle cx="30" cy="14" r="5" fill="none" stroke="#000000" stroke-width="2"/>

  <!-- Head (white fill covers ear strokes inside head area) -->
  <ellipse cx="22" cy="24" rx="11" ry="10" fill="#FFFFFF" stroke="#000000" stroke-width="2"/>

  <!-- Left eye -->
  <circle cx="18" cy="22" r="2" fill="#000000"/>

  <!-- Right eye -->
  <circle cx="26" cy="22" r="2" fill="#000000"/>

  <!-- Nose -->
  <ellipse cx="22" cy="27" rx="2.2" ry="1.6" fill="#000000"/>
</svg>
